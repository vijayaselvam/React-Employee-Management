import { createBrowserHistory as history} from 'history';
//Router History to navigate
export default history();

// Install -> npm install --save history
//         -> npm install react-router-dom --save